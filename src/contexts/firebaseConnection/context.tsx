/* eslint-disable @typescript-eslint/no-explicit-any */
import React from 'react';

const empty = undefined;

const FirebaseContext = React.createContext<any>(empty);

export default FirebaseContext;
